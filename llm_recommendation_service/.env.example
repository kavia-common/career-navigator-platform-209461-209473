# LLM Recommendation Service - .env.example
# Copy to .env and set as needed.

# Optional: enable OpenAI-backed generation; otherwise deterministic fallback is used.
OPENAI_API_KEY=
# Optional model name (used for Chat Completions)
OPENAI_MODEL=gpt-4o-mini

# Server config
PORT=8001

# Optional OpenAI API key to enable LLM-based generation.
# If not set, the service will always use deterministic fallback generation.
OPENAI_API_KEY=

# Optional: override the OpenAI model used when OPENAI_API_KEY is set.
# Default is gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Service port (must be 8001 by default per requirements)
PORT=8001
